server:
  port: 8001

spring:
  application:
    name: eureka

eureka:
  instance:
    hostname: eureka-one
  client:
    registerWithEureka: false #把自身当做客户端注册到其他Eureka服务器
    fetchRegistry: false #在本地缓存所有实例注册信息
#    serviceUrl:
#      #defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
#      #设置与Eureka Server交互的地址，查询服务和注册服务都需要依赖这个地址。
#      ##指向另一个注册中心
#      defaultZone: http://peer2:8001/eureka/
